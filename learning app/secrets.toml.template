# .streamlit/secrets.toml
# Copy this file, remove the .template extension, fill in your values.
# Never commit the real secrets.toml to git.
# On Streamlit Community Cloud, paste these into the Secrets panel instead.

NOTES_PASSWORD = "your-password-here"
ANTHROPIC_API_KEY = "sk-ant-..."
